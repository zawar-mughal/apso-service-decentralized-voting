import { Entity, RelationshipMap } from "../../lib";
import BaseCommand from "../../lib/base-command";
export default class Scaffold extends BaseCommand {
    static description: string;
    static examples: string[];
    static flags: {
        help: import("@oclif/core/lib/interfaces").BooleanFlag<void>;
    };
    static args: {};
    scaffoldServer(dir: string, entity: Entity, relationshipMap: RelationshipMap, apiType: string): Promise<void>;
    setupRestFiles(dir: string, entity: Entity, relationshipMap: RelationshipMap): Promise<void>;
    setupGraphqlFiles(dir: string, entity: Entity, relationshipMap: RelationshipMap): Promise<void>;
    run(): Promise<void>;
}
