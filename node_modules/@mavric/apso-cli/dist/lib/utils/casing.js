"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.snakeCase = exports.camelCase = void 0;
const camelCase = (str, firstUpper) => {
    const firstLetter = firstUpper ? str[0].toUpperCase() : str[0].toLowerCase();
    const restOfLetters = str
        .slice(1)
        .replace(/_[A-Za-z]/g, (underLetter) => underLetter[1].toUpperCase());
    return `${firstLetter}${restOfLetters}`;
};
exports.camelCase = camelCase;
const snakeCase = (str) => str[0].toLowerCase() +
    str
        .slice(1, str.length)
        .replace(/[A-Z]/g, (letter) => `_${letter.toLowerCase()}`);
exports.snakeCase = snakeCase;
