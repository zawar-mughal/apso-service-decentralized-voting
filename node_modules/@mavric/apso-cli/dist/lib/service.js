"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createService = void 0;
const tslib_1 = require("tslib");
const file_system_1 = require("./utils/file-system");
const Eta = tslib_1.__importStar(require("eta"));
const path = tslib_1.__importStar(require("path"));
const createService = async (apiBaseDir, entityName) => {
    const File = path.join(apiBaseDir, `${entityName}.service.ts`);
    // Dependencies
    const svcName = `${entityName}Service`;
    const pluralEntityName = `${entityName}s`;
    const data = {
        svcName,
        entityName,
        pluralEntityName,
    };
    Eta.configure({
        views: path.join(__dirname, "templates"),
    });
    const content = await Eta.renderFileAsync("./rest/service-rest", data);
    (0, file_system_1.createFile)(File, content);
};
exports.createService = createService;
