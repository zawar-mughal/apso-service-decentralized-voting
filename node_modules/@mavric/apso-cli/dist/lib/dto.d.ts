import { Entity, Relationship } from "./types";
export interface ComputedField {
    name: string;
    primary?: boolean;
    dataType: string;
}
export declare const createDto: (apiBaseDir: string, entity: Entity, relationships: Relationship[]) => Promise<void>;
